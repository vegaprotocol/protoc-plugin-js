import join from '../../../utils/join.js'

export default function ({ message, path }) {
  return join`
    /// autogenerated by protoc-plugin-js
    import type { PartialDeep } from 'type-fest'

    import type { ${message.name} } from '../${message.name}'
    export function encode(obj?: PartialDeep<${message.name}>,  buf?:  Uint8Array, byteOffset?: number): Uint8Array
  `
}
